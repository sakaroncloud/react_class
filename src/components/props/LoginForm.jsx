import React from 'react'

const LoginForm = ({ label }) => {
    return (
        <div className='size-[300px] border border-gray-200 rounded-xl flex items-center justify-center'>
            {label}
        </div>
    )
}

export default LoginForm